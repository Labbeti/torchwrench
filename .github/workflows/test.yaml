# Template: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python package using Pip

on:
  push:
    branches: [ main, dev** ]
  pull_request:
    branches: [ main, dev** ]

env:
  CACHE_NUMBER: 0  # increase to reset cache manually
  UV_NO_SYNC: 1
  TORCHWRENCH_TMPDIR: "./tmp"

# Cancel workflow if a new push occurs
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  # Never interrupt a main-branch publication between PyPI and GitHub.
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  build-extras:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        include:
          - os: ubuntu-22.04
            python-version: "3.8"
            torch-version: "1.10"
          - os: ubuntu-latest
            python-version: "3.14"
            torch-version: "torch-latest"
          - os: windows-2022
            python-version: "3.8"
            torch-version: "1.10"
          - os: windows-latest
            python-version: "3.14"
            torch-version: "torch-latest"
          - os: macos-15
            python-version: "3.8"
            torch-version: "2.0"
          - os: macos-latest
            python-version: "3.14"
            torch-version: "torch-latest"

    steps:
    # --- INSTALLATIONS ---
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Install uv and set Python ${{ matrix.python-version }}
      uses: astral-sh/setup-uv@v9.0.0
      with:
        python-version: ${{ matrix.python-version }}
        enable-cache: true
        cache-dependency-glob: "pyproject.toml"

    - name: Install package
      if: matrix.torch-version == 'torch-latest'
      shell: bash
      run: |
        uv venv --python ${{ matrix.python-version }}
        uv pip install .[tests,extras]

    - name: Install package with PyTorch ${{ matrix.torch-version }}
      if: matrix.torch-version != 'torch-latest'
      shell: bash
      run: |
        uv venv --python ${{ matrix.python-version }}
        uv pip install torch==${{ matrix.torch-version }} -e .[tests,extras]

    - name: Additional install soundfile (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get install libsndfile1

    - name: Additional install soundfile (macOS)
      if: matrix.os == 'macos-latest' || matrix.os == 'macos-15'
      run: |
        brew install libsndfile

    # Linux
    - name: Install FFmpeg on Linux
      if: runner.os == 'Linux'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install --yes ffmpeg

    # macOS
    - name: Install FFmpeg on macOS
      if: runner.os == 'macOS'
      shell: bash
      run: |
        brew install ffmpeg

        # Help native extensions find Homebrew libraries.
        echo "$(brew --prefix ffmpeg)/bin" >> "$GITHUB_PATH"
        echo "DYLD_FALLBACK_LIBRARY_PATH=$(brew --prefix ffmpeg)/lib" >> "$GITHUB_ENV"

    # Windows: TorchCodec needs a shared FFmpeg build, not only ffmpeg.exe.
    - name: Install shared FFmpeg on Windows
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        $ErrorActionPreference = "Stop"

        $version = "7.1.1"
        $archive = Join-Path $env:RUNNER_TEMP "ffmpeg.zip"
        $installDir = Join-Path $env:RUNNER_TEMP "ffmpeg"

        # Use a full shared build containing avcodec/avformat DLLs.
        $url = "https://github.com/GyanD/codexffmpeg/releases/download/$version/ffmpeg-$version-full_build-shared.zip"

        Invoke-WebRequest `
          -Uri $url `
          -OutFile $archive

        Expand-Archive `
          -Path $archive `
          -DestinationPath $installDir

        $bin = Get-ChildItem `
          -Path $installDir `
          -Directory `
          -Recurse |
          Where-Object {
            Test-Path (Join-Path $_.FullName "ffmpeg.exe")
          } |
          Select-Object -First 1 -ExpandProperty FullName

        if (-not $bin) {
          throw "Could not locate the extracted FFmpeg bin directory."
        }

        $bin | Out-File `
          -FilePath $env:GITHUB_PATH `
          -Encoding utf8 `
          -Append

        "FFMPEG_BIN=$bin" | Out-File `
          -FilePath $env:GITHUB_ENV `
          -Encoding utf8 `
          -Append

    # --- TESTS ---
    - name: Verify FFmpeg
      run: |
        ffmpeg -version

    - name: Print install info
      run: |
        uv run torchwrench-info

    - name: Check format with Ruff
      run: |
        uv run ruff check

    - name: Test with pytest
      run: |
        uv run coverage run -m pytest -v

    - name: Show coverage results
      run: |
        uv run coverage report -m

  build-minimal:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-24.04]
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13", "3.14"]
        torch-version: ["torch-latest"]
        include:
          - os: windows-2022
            python-version: "3.8"
            torch-version: "1.10"
          - os: windows-latest
            python-version: "3.14"
            torch-version: "torch-latest"
          - os: macos-15
            python-version: "3.9"
            torch-version: "1.10"
          - os: macos-latest
            python-version: "3.14"
            torch-version: "torch-latest"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "1.10"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "1.11"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "1.12"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "1.13"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "2.0"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "2.1"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "2.2"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "2.3"
          - os: ubuntu-24.04
            python-version: "3.8"
            torch-version: "2.4"
            # note: torch 2.5 is not available for python<=3.8
          - os: ubuntu-24.04
            python-version: "3.9"
            torch-version: "2.5"
          - os: ubuntu-24.04
            python-version: "3.9"
            torch-version: "2.6"
          - os: ubuntu-24.04
            python-version: "3.9"
            torch-version: "2.7"
          - os: ubuntu-24.04
            python-version: "3.9"
            torch-version: "2.8"
          - os: ubuntu-24.04
            python-version: "3.10"
            torch-version: "2.9"
          - os: ubuntu-24.04
            python-version: "3.10"
            torch-version: "2.10"
          - os: ubuntu-24.04
            python-version: "3.10"
            torch-version: "2.11"
          - os: ubuntu-24.04
            python-version: "3.10"
            torch-version: "2.12"
          - os: ubuntu-24.04
            python-version: "3.10"
            torch-version: "2.13"

    steps:
    # --- INSTALLATIONS ---
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Install uv and set Python ${{ matrix.python-version }}
      uses: astral-sh/setup-uv@v9.0.0
      with:
        python-version: ${{ matrix.python-version }}
        enable-cache: true
        cache-dependency-glob: "pyproject.toml"

    - name: Install package
      if: matrix.torch-version == 'torch-latest'
      shell: bash
      run: |
        uv venv --python ${{ matrix.python-version }}
        uv pip install .[tests]

    - name: Install package with PyTorch ${{ matrix.torch-version }}
      if: matrix.torch-version != 'torch-latest'
      shell: bash
      run: |
        uv venv --python ${{ matrix.python-version }}
        uv pip install torch==${{ matrix.torch-version }} -e .[tests]

    - name: Additional install soundfile (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get install libsndfile1

    - name: Additional install soundfile (macOS)
      if: matrix.os == 'macos-latest' || matrix.os == 'macos-15'
      run: |
        brew install libsndfile

    # --- TESTS ---
    - name: Print install info
      run: |
        uv run torchwrench-info

    - name: Check format with Ruff
      run: |
        uv run ruff check

    - name: Test with coverage + pytest
      run: |
        uv run coverage run --debug=trace -m pytest -v '--ignore-glob=tests/**test_extras*.py'

    - name: Show coverage results
      run: |
        uv run coverage report -m

  release-check:
    name: Check whether a release is needed
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [build-extras, build-minimal]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      version: ${{ steps.release.outputs.version }}
      should-publish: ${{ steps.release.outputs.should-publish }}

    steps:
    - name: Checkout repository and tags
      uses: actions/checkout@v5
      with:
        fetch-depth: 0

    - name: Validate release metadata
      id: release
      shell: python
      run: |
        import os
        import pathlib
        import re

        init_text = pathlib.Path("src/torchwrench/__init__.py").read_text(encoding="utf-8")
        version_match = re.search(
            r'^__version__\s*=\s*["\'](\d+\.\d+\.\d+)["\']\s*$',
            init_text,
            flags=re.MULTILINE,
        )
        if version_match is None:
            raise SystemExit(
                "__version__ must be a stable X.Y.Z release in "
                "src/torchwrench/__init__.py"
            )

        version = version_match.group(1)
        changelog = pathlib.Path("CHANGELOG.md").read_text(encoding="utf-8")
        heading = rf"^## \[{re.escape(version)}\](?:[ \t]+[^\r\n]*)?$"
        if re.search(heading, changelog, flags=re.MULTILINE) is None:
            raise SystemExit(f"CHANGELOG.md has no release section for {version}")

        tag_ref = pathlib.Path(".git/refs/tags") / f"v{version}"
        tag_exists = tag_ref.exists()
        if not tag_exists:
            import subprocess

            tag_exists = subprocess.run(
                ["git", "rev-parse", "--verify", "--quiet", f"refs/tags/v{version}"],
                check=False,
            ).returncode == 0

        with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
            print(f"version={version}", file=output)
            print(f"should-publish={'false' if tag_exists else 'true'}", file=output)

        if tag_exists:
            print(f"v{version} already exists; publication will be skipped.")
        else:
            print(f"v{version} is ready to publish.")

  publish:
    name: Publish to PyPI and GitHub Releases
    if: needs.release-check.outputs.should-publish == 'true'
    needs: release-check
    runs-on: ubuntu-24.04
    environment:
      name: pypi
      url: https://pypi.org/project/torchwrench/${{ needs.release-check.outputs.version }}/
    permissions:
      contents: write
      id-token: write
    env:
      RELEASE_VERSION: ${{ needs.release-check.outputs.version }}

    steps:
    - name: Checkout tested commit
      uses: actions/checkout@v5

    - name: Install uv
      uses: astral-sh/setup-uv@v9.0.0

    - name: Build distributions
      run: uv build

    - name: Validate distributions
      run: uvx twine check dist/*

    - name: Extract release notes from changelog
      shell: python
      run: |
        import os
        import pathlib
        import re

        version = os.environ["RELEASE_VERSION"]
        changelog = pathlib.Path("CHANGELOG.md").read_text(encoding="utf-8")
        section = re.search(
            rf"^## \[{re.escape(version)}\](?:[ \t]+[^\r\n]*)?\n(.*?)(?=^## \[|\Z)",
            changelog,
            flags=re.MULTILINE | re.DOTALL,
        )
        if section is None:
            raise SystemExit(f"Could not extract changelog notes for {version}")
        pathlib.Path("release-notes.md").write_text(
            section.group(1).strip() + "\n",
            encoding="utf-8",
        )

    - name: Publish distributions to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        skip-existing: true

    - name: Create GitHub Release and tag
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        gh release create "v${RELEASE_VERSION}" dist/* \
          --target "${GITHUB_SHA}" \
          --title "v${RELEASE_VERSION}" \
          --notes-file release-notes.md
